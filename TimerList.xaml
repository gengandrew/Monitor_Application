<Window x:Class="TimeTracker.TimerList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeTracker"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="TimerList" Height="600" Width="595" ResizeMode="CanMinimize">
    <Grid>
        <DataGrid Background="#404040" x:Name="TimerGrid" IsReadOnly="True" AutoGeneratedColumns="AutoGeneratedColumns">
            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="Background" Value="#989898"/>
                    <Setter Property="FontSize" Value="13.5"/>
                    <Setter Property="FontWeight" Value="DemiBold"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="#282828"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
                <Style TargetType="{x:Type DataGridCell}">
                    <Setter Property="Background" Value="LightGray"/>
                    <Setter Property="Height" Value="25"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="#282828"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </DataGrid.Resources>
        </DataGrid>
    </Grid>
</Window>
